#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ifstream a("a.txt"), b("b.txt");
    ofstream out("merged.txt");
    out << a.rdbuf() << b.rdbuf();
    cout << "Merged\n";
}
